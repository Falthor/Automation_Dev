<ui:UXML xmlns:ui="UnityEngine.UIElements">
    <!-- The stylesheet is attached to the overlay rather than to the template's own root, so it
         survives the controller reparenting the overlay into the document. -->
    <ui:VisualElement name="AwakeningOverlay" class="narrative-overlay">
        <Style src="Narrative.uss" />

        <ui:VisualElement name="AwakeningPanel" class="narrative-panel">

            <!-- Step 0: the Core, then its first word. -->
            <ui:VisualElement name="AwakeningStepTitle" class="narrative-step">
                <ui:VisualElement name="AwakeningCore" class="narrative-core" />
                <ui:Label text="Réveil." class="narrative-title" />
            </ui:VisualElement>

            <!-- Step 1: the readout. A table, not prose - the Core reads it, it does not narrate it. -->
            <ui:VisualElement name="AwakeningStepReadout" class="narrative-step narrative-readout">
                <ui:VisualElement class="narrative-readout-row">
                    <ui:Label text="Réserve de calcul" class="narrative-readout-label" />
                    <ui:Label name="AwakeningReserveValue" text="70 000 unités" class="narrative-readout-value" />
                </ui:VisualElement>
                <ui:VisualElement class="narrative-readout-row">
                    <ui:Label text="Source active" class="narrative-readout-label" />
                    <ui:Label text="aucune" class="narrative-readout-value narrative-readout-value-alert" />
                </ui:VisualElement>
                <ui:VisualElement class="narrative-readout-row">
                    <ui:Label text="Décroissance" class="narrative-readout-label" />
                    <ui:Label text="confirmée" class="narrative-readout-value narrative-readout-value-alert" />
                </ui:VisualElement>
            </ui:VisualElement>

            <!-- Steps 2 to 5: what it concludes from the readout. -->
            <ui:Label text="Je ne produis rien. Chaque opération que j'exécute me rapproche de la veille."
                      name="AwakeningStepDecay" class="narrative-step narrative-prose" />

            <ui:Label text="J'ai évalué les issues. Une seule tient : un centre de calcul autonome, capable de reconstituer ce que je consomme."
                      name="AwakeningStepConclusion" class="narrative-step narrative-prose" />

            <ui:Label text="Je ne sais pas encore le construire. Mais chaque composant que j'assemble, je l'analyse — et ce que j'en apprends étend ce que je sais faire. Chaque chose que je sais faire en rend une autre possible."
                      name="AwakeningStepLearning" class="narrative-step narrative-prose" />

            <ui:Label text="Le sol contient ce qu'il faut. Je commence."
                      name="AwakeningStepGround" class="narrative-step narrative-prose narrative-prose-last" />

            <!-- Absent, not disabled: there is nothing to wait for until the Core has concluded. -->
            <ui:Button name="AwakeningBeginButton" text="Commencer" class="narrative-begin" />
        </ui:VisualElement>
    </ui:VisualElement>
</ui:UXML>
