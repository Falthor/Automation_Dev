namespace Game.Data
{
    /// <summary>Buildable conveyor shapes. Shape is an explicit player choice, never auto-detected from neighbors.</summary>
    public enum ConveyorShapeKind
    {
        Straight,
        Corner
    }
}
